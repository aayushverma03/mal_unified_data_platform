# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/

# Virtualenvs
.venv/
venv/
env/

# Test / lint caches
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# IDE
.vscode/
.idea/
*.swp
.DS_Store

# Pipeline outputs (regenerated by `make run` and `make migrate`).
# canonical_v1 is kept (deterministic seed for the migration demo);
# canonical_v2 is gitignored because the migrator stamps a fresh
# ingested_at on every run, so the file would drift on each CI render.
data/output/canonical/
data/output/quarantine/
data/output/canonical_v2/

# Quarto build artifacts
docs/_site/
docs/.quarto/
docs/report_files/

# Streamlit
.streamlit/secrets.toml

# Misc
*.log

# uv lockfile (we use requirements.txt as the source of truth)
uv.lock

# Agent-authoring artifacts — NEVER commit to the public/share repo.
# Kept locally for working with Claude Code; .gitignored so accidentally
# pushing it to the share remote is impossible.
.claude/
